import { useState } from "react"

const list = () =>{
    return <div className="context">
    <h2>待辦事項</h2>
    {
        // listData.map((item) => {
            // const { note, id} = item
            // return <item key={id} id={id} />  
        // })
    }
  </div>
}

export default list